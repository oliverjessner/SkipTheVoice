import QRCode from"qrcode";import{application,apiError,webContext}from"@/lib/api";export const dynamic="force-dynamic";export async function GET(_:Request,{params}:{params:Promise<{connectionId:string}>}){try{const ctx=await webContext(),view=application().whatsapp.getAuthenticationView(ctx,(await params).connectionId);return Response.json(view?{...view,qr:view.qr?await QRCode.toDataURL(view.qr):undefined}:null)}catch(error){return apiError(error)}}
