import{z}from"zod";import{application,apiError,assertCsrf,webContext}from"@/lib/api";const body=z.object({mode:z.enum(["qr","pairing"]).default("qr"),phoneNumber:z.string().optional()});export async function POST(request:Request,{params}:{params:Promise<{connectionId:string}>}){try{await assertCsrf(request);const value=body.parse(await request.json()),ctx=await webContext(),id=(await params).connectionId;await application().connect(ctx,id,{qr:value.mode==="qr",pairingCode:value.mode==="pairing",...(value.phoneNumber?{phoneNumber:value.phoneNumber}:{})});return Response.json({status:"connecting",id})}catch(error){return apiError(error)}}
