import{z}from"zod";import{application,apiError,webContext}from"@/lib/api";const query=z.object({conversationId:z.string().optional(),contactId:z.string().optional(),connectionId:z.string().optional(),status:z.string().optional(),limit:z.coerce.number().int().min(1).max(100).default(20),offset:z.coerce.number().int().min(0).default(0)});export const dynamic="force-dynamic";export async function GET(request:Request){try{const parsed=query.parse(Object.fromEntries(new URL(request.url).searchParams)),values=Object.fromEntries(Object.entries(parsed).filter(([,value])=>value!==undefined));return Response.json(application().listVoiceMessages(await webContext(),values),{headers:{"Cache-Control":"no-store"}})}catch(error){return apiError(error)}}
